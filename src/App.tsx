/**
 * Application composition root.
 *
 * Context providers, routing, and pages are mounted here as feature work lands.
 * At the foundation stage this renders the page surface shell only — no feature
 * UI is implemented yet.
 */
export function App() {
  return <div className="min-h-dvh bg-surface-page" />
}
